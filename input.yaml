input:
  python:
    exe: python3
    name: data
    script: |
      data = list(range(100))
      def fetch():
        import requests
        import time
        time.sleep(1)
        return requests.get("https://api.ipify.org").text

output:
  stdout: {}
